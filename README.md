# STARmeter / The Great Dive

A browser-based shot authoring tool for the STARmeter scroll sequence. The opening actor, credits, rank, and portrait can be replaced locally; the move can be timed and previewed before exporting the finished shot.

## Local development

```sh
npm install
npm run dev
```

GitHub Pages builds and publishes the production site automatically from `main`.
